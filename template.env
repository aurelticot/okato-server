# Optional, development by default, but ensure it is set to Production on the Prod deploy
#NODE_ENV=development

# Optional, if not set the value 9000 will be used
#PORT=9000

# Optional, if not set it will be computed based on the CPUs
# WEB_CONCURRENCY=4

# Optional but recommended on remote deployments to identify the deployment
#ENV_TYPE=staging

# Optional but recommended on remote deployments to identify the deployment
ENV_ID=local-dev

# Optional, can be used to control the log level independently from the NODE_ENV
# If not specified, log level default to 'info' in production, 'debug' otherwise
# "error" | "warn" | "info" | "verbose" | "debug" | "silly"
#LOG_LEVEL=info

# Optional, default is 'true' to enable rate limit, set 'false' to disable
#ENABLE_RATE_LIMITE=false

# Optional, define the request rate limitation per minute, default to 10
# RATE_LIMIT=10

# Optional, default is 'true' to enable CORS, set 'false' to disable
#ENABLE_CORS=true

# Required if ENABLE_CORS=true to allow requests.
# Regex allowed without the surrounding //
#ALLOWED_DOMAINS=http://example1.com, \.example2\.com$

# Optional API key for Raygun Crash reporting service
#RAYGUN_API_KEY=

#Optional Source Token for Logtail
#LOGTAIL_SOURCE_TOKEN=